// define an array of fruits
const fruits = ['Apple', 'Orange', 'Grape', 'Mandarin', 'Banana', 'Strawberry'];
// .forEach((fruit, index) => {
// console.log() a message interpolating the name of the fruit and it's position in the array (index increased by 1);
// })
fruits.forEach((element, index) => console.log(index + 1, element));
